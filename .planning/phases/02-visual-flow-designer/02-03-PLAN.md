---
phase: 02-visual-flow-designer
plan: 03
type: execute
wave: 3
depends_on: ["02-02"]
files_modified:
  - frontend/src/components/flow/PropertyPanel.tsx
  - frontend/src/components/flow/panels/SIPInstancePanel.tsx
  - frontend/src/components/flow/panels/CommandPanel.tsx
  - frontend/src/components/flow/panels/EventPanel.tsx
  - frontend/src/components/flow/FlowCanvas.tsx
autonomous: true

must_haves:
  truths:
    - "Clicking a node opens a slide-out property panel on the right"
    - "SIP Instance panel shows server select (mock) and transport select (UDP/TCP/TLS)"
    - "Command panel shows command-specific form fields (e.g., target URI for MakeCall)"
    - "Event panel shows event type and timeout configuration"
    - "Save button persists changes to node data in store"
    - "Cancel button discards changes and closes panel"
    - "Clicking outside panel (overlay) triggers cancel"
  artifacts:
    - path: "frontend/src/components/flow/PropertyPanel.tsx"
      provides: "Sheet-based property panel that routes to type-specific sub-panels"
      contains: "Sheet"
    - path: "frontend/src/components/flow/panels/SIPInstancePanel.tsx"
      provides: "SIP Instance property form with server + transport selects"
      contains: "Select"
    - path: "frontend/src/components/flow/panels/CommandPanel.tsx"
      provides: "Command-specific property forms based on command type"
      contains: "CommandType"
    - path: "frontend/src/components/flow/panels/EventPanel.tsx"
      provides: "Event property form with type and timeout"
      contains: "timeout"
  key_links:
    - from: "frontend/src/components/flow/PropertyPanel.tsx"
      to: "frontend/src/stores/flowStore.ts"
      via: "useFlowStore selectedNodeId selector and updateNodeData action"
      pattern: "selectedNodeId"
    - from: "frontend/src/components/flow/PropertyPanel.tsx"
      to: "shadcn/ui Sheet"
      via: "Sheet open controlled by selectedNodeId"
      pattern: "Sheet"
---

<objective>
Create the right-side slide-out property panel using shadcn/ui Sheet with save/cancel workflow. Implement type-specific property forms for SIP Instance, Command, and Event nodes.

Purpose: Enable users to configure node properties after placing them on canvas, completing the design workflow.
Output: Working property panel that opens on node click, shows correct form per node type, and saves/cancels changes.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/02-visual-flow-designer/02-RESEARCH.md
@.planning/phases/02-visual-flow-designer/02-CONTEXT.md
@.planning/phases/02-visual-flow-designer/02-02-SUMMARY.md
@frontend/src/stores/flowStore.ts
@frontend/src/types/nodes.ts
@frontend/src/components/flow/FlowCanvas.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install shadcn/ui components and create property panel forms</name>
  <files>
    frontend/src/components/flow/panels/SIPInstancePanel.tsx
    frontend/src/components/flow/panels/CommandPanel.tsx
    frontend/src/components/flow/panels/EventPanel.tsx
  </files>
  <action>
    1. Install required shadcn/ui components:
       ```bash
       cd frontend && npx shadcn@latest add sheet select input label button
       ```
       (Some may already be installed from Phase 1 -- the CLI handles duplicates gracefully.)

    2. Create `frontend/src/components/flow/panels/SIPInstancePanel.tsx`:
       - Props: { data: SIPInstanceNodeData, onChange: (data: SIPInstanceNodeData) => void }
       - Server Select: dropdown with mock servers ["Server 1 (192.168.1.100:5060)", "Server 2 (10.0.0.50:5060)"]
         - Note: Phase 4 will replace with real server list
       - Transport Select: dropdown with UDP, TCP, TLS options
       - Use Label + Select components from shadcn/ui
       - Layout: vertical stack with space-y-4

    3. Create `frontend/src/components/flow/panels/CommandPanel.tsx`:
       - Props: { data: CommandNodeData, onChange: (data: CommandNodeData) => void }
       - Render different form fields based on data.command:
         - makeCall: Input for "Target URI" (SIP URI to call, e.g., sip:user@domain)
         - bye: no additional fields (just label display)
         - cancel: no additional fields
         - hold: no additional fields (operates on active session)
         - retrieve: no additional fields (operates on held session)
         - blindTransfer: Input for "Transfer To" (target URI)
         - muteTransfer: Input for "Transfer To" (target URI)
         - busy: no additional fields (auto-responds 486)
       - Show command type as read-only badge at top
       - Use Label + Input from shadcn/ui

    4. Create `frontend/src/components/flow/panels/EventPanel.tsx`:
       - Props: { data: EventNodeData, onChange: (data: EventNodeData) => void }
       - Event Type: Select with options ["Incoming Call", "Call Answered", "Call Ended", "Call Failed", "Transfer Complete"]
       - Timeout: Input (number, in seconds, default 30)
       - Use Label + Select + Input from shadcn/ui
  </action>
  <verify>
    ```bash
    cd frontend && npx tsc --noEmit
    ```
    All panel components compile without errors.
  </verify>
  <done>
    - SIPInstancePanel renders server + transport selects
    - CommandPanel renders command-specific fields based on command type
    - EventPanel renders event type select + timeout input
    - All panels use shadcn/ui components consistently
  </done>
</task>

<task type="auto">
  <name>Task 2: Create PropertyPanel Sheet wrapper and wire to FlowCanvas</name>
  <files>
    frontend/src/components/flow/PropertyPanel.tsx
    frontend/src/components/flow/FlowCanvas.tsx
  </files>
  <action>
    1. Create `frontend/src/components/flow/PropertyPanel.tsx`:
       - Import Sheet, SheetContent, SheetHeader, SheetTitle, SheetFooter from shadcn/ui
       - Import Button from shadcn/ui
       - Import useFlowStore with selectors
       - Import all three panel components
       - Get selectedNodeId from store, find selectedNode from nodes array
       - Use useState for localData (copy of selectedNode.data when panel opens)
       - useEffect: when selectedNodeId changes, reset localData to selectedNode.data
       - Sheet open = !!selectedNodeId
       - Sheet onOpenChange: when closing (open=false), treat as cancel
       - SheetContent side="right" className="w-[400px] sm:w-[450px]"
       - SheetHeader: title based on node type ("SIP Instance Properties", "Command Properties", "Event Properties")
       - Body: render appropriate panel component based on selectedNode.type, pass localData + onChange setter
       - SheetFooter: Cancel button (variant="outline") + Save button
       - handleSave: call actions.updateNodeData(selectedNodeId, localData), then setSelectedNode(null)
       - handleCancel: call actions.setSelectedNode(null) (discard changes)

    2. Update `frontend/src/components/flow/FlowCanvas.tsx` (or App.tsx):
       - Import and render PropertyPanel component alongside FlowCanvas
       - PropertyPanel should be rendered at the same level as FlowCanvas (sibling, not child)
       - Since Sheet uses portal, it can be rendered anywhere
       - Add PropertyPanel import and render in App.tsx after FlowCanvas
  </action>
  <verify>
    ```bash
    cd frontend && npx tsc --noEmit && npm run build
    ```
    Build succeeds. PropertyPanel renders Sheet with correct sub-panel per node type.
  </verify>
  <done>
    - Clicking a node opens Sheet from right side showing correct property form
    - Save button updates node data in store and closes panel
    - Cancel button (or clicking outside) discards changes and closes panel
    - Local form state is independent from store until Save
    - Different node types show different property forms
    - Vite build succeeds
  </done>
</task>

</tasks>

<verification>
1. `cd frontend && npm run build` — Build succeeds
2. `grep "Sheet" frontend/src/components/flow/PropertyPanel.tsx` — Sheet component used
3. `grep "updateNodeData" frontend/src/components/flow/PropertyPanel.tsx` — Save wired to store
4. `grep "selectedNodeId" frontend/src/components/flow/PropertyPanel.tsx` — Sheet controlled by selection
5. `grep "PropertyPanel" frontend/src/App.tsx` — Panel rendered in layout
</verification>

<success_criteria>
- Node click opens right slide-out property panel
- Panel shows correct form based on node type (SIP Instance / Command / Event)
- Command panel shows different fields per command type
- Save persists changes to store, Cancel discards
- Panel closes on save, cancel, or outside click
- Build succeeds
</success_criteria>

<output>
After completion, create `.planning/phases/02-visual-flow-designer/02-03-SUMMARY.md`
</output>
