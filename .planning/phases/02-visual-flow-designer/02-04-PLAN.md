---
phase: 02-visual-flow-designer
plan: 04
type: execute
wave: 4
depends_on: ["02-03"]
files_modified:
  - frontend/src/components/flow/FlowCanvas.tsx
  - frontend/src/stores/flowStore.ts
autonomous: false

must_haves:
  truths:
    - "Invalid edge connections (wrong node type sequence) show red warning color"
    - "Valid edge connections show normal color with arrow"
    - "Edge validation checks flow rules: Instance->Command, Command->Event/Command, Event->Command"
    - "Canvas is responsive with 100+ nodes"
    - "Complete flow design workflow works end-to-end: drag nodes, connect edges, configure properties"
  artifacts:
    - path: "frontend/src/components/flow/FlowCanvas.tsx"
      provides: "Edge validation logic integrated into onConnect"
      contains: "isValid"
  key_links:
    - from: "frontend/src/stores/flowStore.ts"
      to: "frontend/src/components/flow/edges/FlowEdge.tsx"
      via: "Edge data.isValid flag set during onConnect"
      pattern: "isValid"
---

<objective>
Add edge validation with visual warnings (red coloring for invalid connections), and perform end-to-end human verification of the complete visual flow designer.

Purpose: Complete the Phase 2 visual flow designer with edge validation and verify all success criteria are met.
Output: Fully functional visual flow designer with edge validation, verified by human testing.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/02-visual-flow-designer/02-RESEARCH.md
@.planning/phases/02-visual-flow-designer/02-CONTEXT.md
@.planning/phases/02-visual-flow-designer/02-03-SUMMARY.md
@frontend/src/stores/flowStore.ts
@frontend/src/components/flow/FlowCanvas.tsx
@frontend/src/components/flow/edges/FlowEdge.tsx
@frontend/src/types/nodes.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add edge validation with visual warnings</name>
  <files>
    frontend/src/stores/flowStore.ts
    frontend/src/components/flow/FlowCanvas.tsx
  </files>
  <action>
    1. Create edge validation utility (can be in flowStore.ts or separate file):
       - Define validSequences map:
         ```
         sipInstance -> ['command']
         command -> ['event', 'command']
         event -> ['command']
         ```
       - validateConnection(sourceType: string, targetType: string): boolean
         - Look up sourceType in validSequences
         - Return true if targetType is in allowed targets
         - Return false otherwise

    2. Update the onConnect action in flowStore.ts:
       - When creating a new edge, look up source and target node types
       - Call validateConnection to determine if the edge is valid
       - Set edge data.isValid = validationResult
       - Edge type should be 'flowEdge' (custom edge with arrow + color)
       - This makes FlowEdge component automatically render red for invalid connections

    3. Also add validation for edges when nodes are added or types change:
       - In addNode, no edge revalidation needed (new node has no edges)
       - The existing FlowEdge component already reads data.isValid for coloring

    4. Ensure FlowCanvas passes defaultEdgeOptions={{ type: 'flowEdge' }} to ReactFlow
       so that edges created via handle drag also use the custom edge type.

    5. Keep isValidConnection in FlowCanvas to only block self-connections.
       All other connections are allowed but marked visually if invalid.
  </action>
  <verify>
    ```bash
    cd frontend && npx tsc --noEmit && npm run build && \
    grep "validateConnection" frontend/src/stores/flowStore.ts && \
    grep "isValid.*=" frontend/src/stores/flowStore.ts
    ```
    Build succeeds with edge validation logic integrated. validateConnection function exists in flowStore.ts and sets isValid on edge data.
  </verify>
  <done>
    - Invalid connections (e.g., Event->SIPInstance) create red-colored edges
    - Valid connections (e.g., SIPInstance->Command) create normal-colored edges
    - Self-connections are blocked entirely (isValidConnection)
    - All edges have arrow markers
    - validateConnection function exists and sets data.isValid
    - Build succeeds
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    Complete Visual Flow Designer (Phase 2) with:
    - ReactFlow canvas with grid background, zoom controls, minimap
    - Left sidebar with 3 node groups (SIP Instance, Commands, Events) - drag to add
    - 3 custom node types with lucide icons and color-coded borders
    - Edge connections with arrows and red warning for invalid sequences
    - Right slide-out property panel with save/cancel for each node type
    - SIP Instance panel reads server list from serverStore
    - Sidebar toggle in header
  </what-built>
  <how-to-verify>
    Run the app:
    ```bash
    cd /Users/kyh0703/Project/sipflow && wails dev
    ```

    Test all Phase 2 success criteria:

    1. **FLOW-01 - SIP Instance nodes**: Drag "SIP UA" from sidebar onto canvas. Click it. Verify right panel shows server select populated from store (Dev Server, Staging Server, Production Server) + transport select. Save properties.

    2. **FLOW-02 - Command nodes**: Drag each command type (MakeCall, Hold, Retrieve, Blind Transfer, Mute Transfer, Bye, Cancel, 486 Busy) onto canvas. Verify each shows correct icon. Click MakeCall node, verify Target URI field in property panel.

    3. **FLOW-03 - Event nodes**: Drag "Wait Event" onto canvas. Click it. Verify event type select + timeout field in property panel.

    4. **FLOW-04 - Edge connections**: Connect SIP Instance -> MakeCall (should be normal color). Connect MakeCall -> Wait Event (normal). Try connecting Event -> SIP Instance (should be RED warning). Verify arrow direction.

    5. **Performance**: Add 20+ nodes by dragging rapidly. Canvas should remain responsive (smooth pan/zoom). If possible test with more.

    6. **General UX**: Toggle sidebar open/closed. Verify canvas resizes. Pan and zoom canvas. Use minimap to navigate. Verify empty canvas message appears with no nodes.
  </how-to-verify>
  <resume-signal>Type "approved" if all criteria pass, or describe issues found.</resume-signal>
</task>

</tasks>

<verification>
1. `cd frontend && npm run build` -- Final build succeeds
2. All 5 Phase 2 success criteria verified by human testing
3. Edge validation visually distinguishes valid/invalid connections
4. Property panel saves/cancels correctly for all node types
5. SIP Instance panel server list comes from serverStore
</verification>

<success_criteria>
- User can drag all 3 node types onto canvas and configure properties (FLOW-01, FLOW-02, FLOW-03)
- Nodes connect with directional edges, invalid sequences shown in red (FLOW-04)
- Canvas remains responsive with many nodes (performance)
- Complete design workflow: drag -> place -> connect -> configure -> save
- Human verification approved
</success_criteria>

<output>
After completion, create `.planning/phases/02-visual-flow-designer/02-04-SUMMARY.md`
</output>
