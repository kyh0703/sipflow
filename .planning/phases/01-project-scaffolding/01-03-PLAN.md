---
phase: 01-project-scaffolding
plan: 03
type: execute
wave: 3
depends_on: ["01-01", "01-02"]
files_modified:
  - frontend/src/App.tsx
  - frontend/src/App.css
autonomous: false

must_haves:
  truths:
    - "프론트엔드에서 Go 백엔드의 Ping 메서드를 호출하면 pong을 수신한다"
    - "프론트엔드에서 Go 백엔드의 GetVersion 메서드를 호출하면 버전 문자열을 수신한다"
    - "Tailwind CSS 유틸리티 클래스가 화면에 올바르게 렌더링된다"
    - "wails dev로 앱이 정상 실행된다"
  artifacts:
    - path: "frontend/src/App.tsx"
      provides: "Go 바인딩 테스트 UI, Ping/GetVersion 호출, Tailwind 스타일링"
      contains: "Ping"
      min_lines: 20
  key_links:
    - from: "frontend/src/App.tsx"
      to: "internal/binding/engine_binding.go"
      via: "wailsjs/go/binding/EngineBinding 자동생성 바인딩을 통한 Ping() 호출"
      pattern: "import.*EngineBinding"
    - from: "frontend/src/App.tsx"
      to: "frontend/src/index.css"
      via: "Tailwind 유틸리티 클래스 사용 (className)"
      pattern: "className.*text-|bg-|flex|p-"
---

<objective>
Go-React 양방향 통신을 검증하고 전체 스택이 올바르게 동작하는지 확인한다.

Purpose: Phase 1의 최종 성공 기준인 "wails dev로 앱 실행, Go-React 양방향 통신 동작 확인"을 달성한다. 이후 Phase 2부터 본격적인 기능 구현을 시작할 수 있는 안정적인 기반을 검증한다.
Output: Ping/Pong 바인딩 테스트가 동작하는 앱 화면, 사용자 검증 완료
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-project-scaffolding/01-RESEARCH.md
@.planning/phases/01-project-scaffolding/01-01-SUMMARY.md
@.planning/phases/01-project-scaffolding/01-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: 바인딩 검증 UI 구현 및 빌드 테스트</name>
  <files>frontend/src/App.tsx, frontend/src/App.css</files>
  <action>
프론트엔드에서 Go 백엔드 바인딩을 호출하는 테스트 UI를 구현한다.

1. frontend/src/App.css 정리:
   - Wails 기본 템플릿의 CSS를 제거하거나 최소화 (Tailwind로 대체)

2. frontend/src/App.tsx 수정:
   - 자동생성된 바인딩 임포트: `import { Ping, GetVersion } from '../wailsjs/go/binding/EngineBinding'`
   - useEffect + useState로 마운트 시 Ping(), GetVersion() 호출
   - 결과를 화면에 표시:
     - 제목: "SIPFLOW" (text-3xl font-bold)
     - 바인딩 상태: "Backend: pong" 또는 에러 메시지
     - 버전: "v0.1.0"
   - Tailwind 유틸리티 클래스 사용 (flex, items-center, justify-center, bg-*, text-*, p-*, rounded-lg, shadow-lg)
   - 01-RESEARCH.md의 Frontend Binding Verification 패턴 참조

3. 빌드 검증:
   - `wails build` 실행하여 전체 빌드 성공 확인
   - 빌드 실패 시 에러 분석 및 수정

안티패턴:
- wailsjs/ 디렉토리 수동 편집 금지 (wails dev/build가 자동 생성)
- any 타입 사용 금지 (자동생성 바인딩의 타입 사용)

참고: wails dev 실행 시 wailsjs/ 바인딩이 자동 생성된다. 바인딩 임포트가 빨간줄이어도 wails build/dev 후 해결된다.
  </action>
  <verify>
- `wails build` 성공
- `grep "Ping" frontend/src/App.tsx` 바인딩 호출 확인
- `grep "GetVersion" frontend/src/App.tsx` 버전 호출 확인
- `grep "tailwindcss" frontend/src/index.css` Tailwind v4 임포트 확인
  </verify>
  <done>App.tsx가 EngineBinding.Ping()과 GetVersion()을 호출하여 결과를 Tailwind 스타일링된 UI에 표시, wails build 성공</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
전체 Wails 프로젝트 스캐폴딩: Go 백엔드(internal/ 패키지 구조, EngineBinding), 프론트엔드(Tailwind v4 + shadcn/ui + xyflow + zustand), Go-React 바인딩 테스트 UI
  </what-built>
  <how-to-verify>
1. 프로젝트 루트에서 `wails dev` 실행
2. 앱 창이 열리면 확인:
   - "SIPFLOW" 제목이 보이는가?
   - "Backend: pong" (또는 유사한 바인딩 성공 메시지)이 표시되는가?
   - 버전 정보 ("v0.1.0" 또는 "0.1.0")가 표시되는가?
   - Tailwind 스타일이 적용되어 있는가? (그림자, 둥근 모서리, 중앙 정렬 등)
3. Ctrl+C로 wails dev 종료
  </how-to-verify>
  <resume-signal>"approved"를 입력하거나 발견된 문제를 설명하세요</resume-signal>
</task>

</tasks>

<verification>
1. `wails build` 전체 빌드 성공
2. App.tsx에서 EngineBinding의 Ping, GetVersion 호출
3. Tailwind 유틸리티 클래스가 App.tsx에 사용됨
4. wails dev로 앱 실행 시 Go-React 통신 동작 (사용자 검증)
</verification>

<success_criteria>
- wails build 성공
- wails dev로 앱 실행 시:
  - Go 백엔드 Ping 호출 -> "pong" 응답 표시
  - Go 백엔드 GetVersion 호출 -> 버전 표시
  - Tailwind CSS 스타일 적용됨
- 사용자가 시각적으로 검증하고 approved
</success_criteria>

<output>
완료 후, `.planning/phases/01-project-scaffolding/01-03-SUMMARY.md` 생성
</output>
