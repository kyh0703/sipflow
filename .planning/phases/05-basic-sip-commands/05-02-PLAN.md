---
phase: 05-basic-sip-commands
plan: 02
type: execute
wave: 2
depends_on: ["05-01"]
files_modified:
  - internal/handler/sip_service.go
  - internal/infra/sip/ua_manager.go
  - app.go
autonomous: true

must_haves:
  truths:
    - "MakeCall initiates SIP INVITE via diago and returns callID immediately"
    - "Bye sends SIP BYE on established call via dialog.Hangup"
    - "Cancel aborts outgoing call via context cancellation (SIP CANCEL)"
    - "Call state changes emit sip:callState events to frontend"
    - "One call per UA enforced (MakeCall rejects if node has active call)"
    - "ServeBackground runs before Invite to ensure transport listeners are active"
    - "SIP 4xx/5xx error responses are extracted with status code and reason phrase"
  artifacts:
    - path: "internal/handler/sip_service.go"
      provides: "MakeCall, Bye, Cancel Wails-bindable methods"
      contains: "func (s *SIPService) MakeCall"
    - path: "internal/handler/sip_service.go"
      provides: "SessionManager field on SIPService"
      contains: "sessionManager"
    - path: "internal/infra/sip/ua_manager.go"
      provides: "ServeBackground integration in CreateUA"
      contains: "ServeBackground"
    - path: "app.go"
      provides: "SessionManager wired into SIPService"
      contains: "NewSessionManager"
  key_links:
    - from: "internal/handler/sip_service.go"
      to: "internal/infra/sip/ua_manager.go"
      via: "s.uaManager.GetDiago(nodeID)"
      pattern: "GetDiago"
    - from: "internal/handler/sip_service.go"
      to: "internal/infra/sip/session_manager.go"
      via: "s.sessionManager.Add/Get/Remove"
      pattern: "sessionManager\\."
    - from: "internal/handler/sip_service.go"
      to: "internal/handler/event_emitter.go"
      via: "s.emitter.Emit(sip:callState)"
      pattern: "sip:callState"
    - from: "internal/handler/sip_service.go"
      to: "github.com/emiago/sipgo"
      via: "sipgo.ErrDialogResponse for SIP error code extraction"
      pattern: "ErrDialogResponse"
---

<objective>
Implement MakeCall, Bye, and Cancel SIP command methods on SIPService with proper diago integration.

Purpose: These are the core SIP operations that validate end-to-end SIP integration. MakeCall sends INVITE, Bye sends BYE, Cancel sends CANCEL -- all through diago library with proper state tracking and event emission.
Output: Three new Wails-bindable methods on SIPService, ServeBackground integrated into UAManager, SessionManager wired in.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/05-basic-sip-commands/05-RESEARCH.md
@.planning/phases/05-basic-sip-commands/05-01-SUMMARY.md

@internal/handler/sip_service.go
@internal/handler/event_emitter.go
@internal/handler/response.go
@internal/infra/sip/ua_manager.go
@app.go
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add ServeBackground to UAManager CreateUA</name>
  <files>internal/infra/sip/ua_manager.go</files>
  <action>
The research identifies Pitfall 4: diago may need ServeBackground() running before Invite() can receive SIP responses. Add ServeBackground call in CreateUA after creating the diago instance.

Modify CreateUA to:
1. Store the lifecycle context (not just cancel func) in managedUA struct -- add `ctx context.Context` field to managedUA
2. After creating the diago instance, call `dg.ServeBackground(ctx, diago.WithServeHandler(nil))` or the appropriate nil handler. Check diago API: `dg.ServeBackground(ctx)` may accept variadic options. If ServeBackground requires a handler func for incoming calls, pass a no-op handler since Phase 5 is outbound-only: `func(d *diago.DialogServerSession) {}`.
3. If ServeBackground fails, clean up and return error.

IMPORTANT: Check `dg.ServeBackground` signature from diago source. It likely takes `(ctx context.Context, handler func(*diago.DialogServerSession))`. For outbound-only Phase 5, use a no-op handler.

Also update destroyManagedUA: the context cancel already handles stopping ServeBackground since it uses the same context.

Run existing ua_manager tests to ensure no regressions. The goleak tests will verify no goroutine leaks (ServeBackground goroutine must stop when context is cancelled).
  </action>
  <verify>cd /Users/kyh0703/Project/sipflow && go test ./internal/infra/sip/ -v -run TestCreateAndDestroy_NoGoroutineLeak</verify>
  <done>ServeBackground runs in CreateUA. Existing tests pass. No goroutine leaks on DestroyUA/DestroyAll.</done>
</task>

<task type="auto">
  <name>Task 2: Wire SessionManager and implement MakeCall/Bye/Cancel</name>
  <files>internal/handler/sip_service.go, app.go</files>
  <action>
**Step 1: Update SIPService struct and constructor**

Add `sessionManager *sip.SessionManager` field to SIPService struct.

Update NewSIPService signature to accept SessionManager as third parameter:
```go
func NewSIPService(emitter *EventEmitter, uaManager *sip.UAManager, sessionManager *sip.SessionManager) *SIPService {
    return &SIPService{
        emitter:        emitter,
        uaManager:      uaManager,
        sessionManager: sessionManager,
    }
}
```

Update app.go NewApp() to create SessionManager and pass it to NewSIPService:
```go
sessionManager := sip.NewSessionManager(sipLogger)
sipService := handler.NewSIPService(emitter, uaManager, sessionManager)
```

**Step 2: Implement MakeCall**

Follow the pattern from RESEARCH Pattern 3 exactly:
```go
func (s *SIPService) MakeCall(nodeID, targetURI string) Response[string]
```
- Guard: check s.entClient != nil (NO_PROJECT error)
- Guard: check s.sessionManager.HasActiveCall(nodeID) -- reject with "CALL_ACTIVE" if true (one-call-per-UA)
- Get diago via s.uaManager.GetDiago(nodeID) -- fail with "UA_NOT_FOUND" if error
- Parse target URI with sip.ParseUri("sip:" + targetURI) -- fail with "INVALID_URI" if error
- Generate callID with fmt.Sprintf("%s-%d", nodeID, time.Now().UnixNano()) (simple, no uuid dependency needed)
- Create cancellable context: ctx, cancel := context.WithCancel(context.Background())
- Create ActiveSession with cancel, nodeID, state=CallStateDialing, nil dialog
- Add to sessionManager
- Launch goroutine that:
  - defer s.sessionManager.Remove(callID)
  - Creates InviteOptions with OnResponse callback that:
    - For 180 Ringing: set session.State = CallStateRinging, emit "sip:callState" with state "ringing"
    - For other 1xx provisional (100 Trying, 183 Session Progress): emit "sip:callState" with state "progress" and include statusCode and reason
    - Return nil from callback (no error)
  - Calls dg.Invite(ctx, recipient, opts)
  - On error: extract SIP response details using sipgo.ErrDialogResponse with errors.As():
    - If errors.As(err, &errResp): extract errResp.Res.StatusCode and errResp.Res.Reason
      - Categorize: 4xx client errors (400 Bad Request, 401 Unauthorized, 403 Forbidden, 404 Not Found, 408 Timeout, 486 Busy Here, 487 Request Terminated), 5xx server errors (500, 502, 503)
      - Emit "sip:callState" failed event with: callID, nodeID, state "failed", statusCode (int), reason (string), error (full error message)
    - If errors.Is(err, context.Canceled): emit "sip:callState" with state "cancelled" (user-initiated cancel, not an error)
    - Otherwise: emit "sip:callState" failed with statusCode 0 and raw error message
    - Set session.State = CallStateFailed in all error cases
  - On success: set session.Dialog, set state=Established, emit "sip:callState" established
  - Wait on dialog.Context().Done()
  - Emit "sip:callState" terminated
  - defer dialog.Close()
- Return Success(callID) immediately

**Step 3: Implement Bye**

Follow RESEARCH Pattern 4:
```go
func (s *SIPService) Bye(callID string) Response[bool]
```
- Get session from sessionManager -- "SESSION_NOT_FOUND" if missing
- Check state == CallStateEstablished -- "INVALID_STATE" if not
- Check session.Dialog != nil -- "NO_DIALOG" if nil
- Call session.Dialog.Hangup(ctx) with 5s timeout
- Return Success(true)

**Step 4: Implement Cancel**

Follow RESEARCH Pattern 5:
```go
func (s *SIPService) Cancel(callID string) Response[bool]
```
- Get session from sessionManager -- "SESSION_NOT_FOUND" if missing
- Check state == CallStateDialing || CallStateRinging -- "INVALID_STATE" otherwise
- Call session.Cancel() (context cancellation triggers SIP CANCEL automatically)
- Return Success(true)

**Imports needed in sip_service.go:** Add "errors", "github.com/emiago/diago", "github.com/emiago/sipgo", sipgosip "github.com/emiago/sipgo/sip" (already imported for SIPDebug). Ensure "time" is imported.

**Important:** Do NOT add `"github.com/google/uuid"` -- use simple callID generation instead.
  </action>
  <verify>cd /Users/kyh0703/Project/sipflow && go build ./... 2>&1 | head -30</verify>
  <done>MakeCall/Bye/Cancel compile. SIPService constructor accepts 3 params (emitter, uaManager, sessionManager). app.go creates SessionManager and passes it. MakeCall emits detailed error events with SIP status codes for 4xx/5xx responses. `go build ./...` succeeds with zero errors.</done>
</task>

</tasks>

<verification>
cd /Users/kyh0703/Project/sipflow && go build ./...
cd /Users/kyh0703/Project/sipflow && go test ./internal/infra/sip/ -v
cd /Users/kyh0703/Project/sipflow && go vet ./...
All pass. No compilation errors, no vet warnings.
</verification>

<success_criteria>
- MakeCall(nodeID, targetURI) returns callID immediately, runs Invite in goroutine
- Bye(callID) sends BYE via dialog.Hangup on established calls only
- Cancel(callID) cancels context to trigger SIP CANCEL on dialing/ringing calls only
- sip:callState events emitted for: dialing, ringing, progress, established, failed, cancelled, terminated
- Failed events include SIP statusCode and reason phrase (4xx/5xx extracted via ErrDialogResponse)
- Context cancellation distinguished from SIP errors (cancelled vs failed state)
- One-call-per-UA guard rejects MakeCall if node already has active call
- ServeBackground runs in CreateUA for transport listener readiness
- NewSIPService accepts 3 parameters (emitter, uaManager, sessionManager)
- `go build ./...` succeeds, `go test ./internal/infra/sip/` passes
</success_criteria>

<output>
After completion, create `.planning/phases/05-basic-sip-commands/05-02-SUMMARY.md`
</output>
