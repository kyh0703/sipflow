---
phase: 04-sip-infrastructure
plan: 03
type: execute
wave: 2
depends_on: [04-01, 04-02]
files_modified:
  - internal/handler/sip_service.go
  - app.go
  - frontend/src/stores/serverStore.ts
  - frontend/src/components/SIPTracePanel.tsx
  - frontend/src/components/FlowCanvas.tsx
autonomous: false

must_haves:
  truths:
    - "User can add/edit/delete SIP servers through the UI and they persist in SQLite"
    - "User can start a UA for a SIP Instance node and it creates a diago instance"
    - "User can stop a UA and it terminates gracefully"
    - "User can see SIP trace log messages in a dedicated panel"
    - "All UAs terminate on app shutdown"
  artifacts:
    - path: "internal/handler/sip_service.go"
      provides: "StartUA, StopUA, StopAllUAs, GetUAStatus, ToggleSIPTrace methods"
      contains: "func (s *SIPService) StartUA"
    - path: "frontend/src/stores/serverStore.ts"
      provides: "Backend-synced server store replacing mock data"
      contains: "ListServers"
    - path: "frontend/src/components/SIPTracePanel.tsx"
      provides: "SIP trace log display panel"
      contains: "sip:trace"
  key_links:
    - from: "internal/handler/sip_service.go"
      to: "internal/infra/sip/ua_manager.go"
      via: "uaManager.CreateUA / DestroyUA"
      pattern: "uaManager\\."
    - from: "frontend/src/stores/serverStore.ts"
      to: "internal/handler/sip_service.go"
      via: "Wails Go binding calls"
      pattern: "ListServers|CreateServer|DeleteServer"
    - from: "frontend/src/components/SIPTracePanel.tsx"
      to: "sip:trace event"
      via: "Wails EventsOn"
      pattern: "sip:trace"
---

<objective>
Wire UAManager into SIPService, replace frontend mock serverStore with backend calls, add SIP trace panel, and verify end-to-end.

Purpose: Connects all Phase 4 components: backend CRUD (Plan 01) + UAManager/TraceLogger (Plan 02) + frontend. Delivers all 5 success criteria for the phase.
Output: Full SIP infrastructure working end-to-end -- server config persistence, UA lifecycle management, SIP trace visibility.
</objective>

<execution_context>
@./.claude/agents/gsd-planner.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/04-sip-infrastructure/04-RESEARCH.md
@.planning/phases/04-sip-infrastructure/04-01-SUMMARY.md
@.planning/phases/04-sip-infrastructure/04-02-SUMMARY.md

@internal/handler/sip_service.go (from Plan 01 -- CRUD methods)
@internal/infra/sip/ua_manager.go (from Plan 02 -- UAManager)
@internal/infra/sip/sip_logger.go (from Plan 02 -- SIPTraceHandler)
@app.go
@frontend/src/stores/serverStore.ts (mock data to replace)
@frontend/src/components/FlowCanvas.tsx (layout reference for trace panel placement)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Wire UAManager into SIPService and app lifecycle</name>
  <files>
    internal/handler/sip_service.go
    app.go
  </files>
  <action>
    1. Update `internal/handler/sip_service.go`:
       - Add `uaManager *sip.UAManager` field to SIPService struct
       - Update `NewSIPService` to accept and store uaManager
       - Add UA lifecycle methods:
         - `StartUA(nodeID string, serverID int) Response[bool]`:
           - Guard nil entClient with NO_PROJECT
           - Fetch SIP server config from ent by serverID
           - Build UAConfig from server config (DisplayName: "SIPFlow/1.0", Transport: server.Transport lowercased, BindHost: "0.0.0.0", BindPort: 0)
           - Call uaManager.CreateUA(nodeID, cfg)
           - Return success/error
         - `StopUA(nodeID string) Response[bool]`:
           - Call uaManager.DestroyUA(nodeID)
           - Return success/error
         - `StopAllUAs() Response[bool]`:
           - Call uaManager.DestroyAll()
           - Return success
         - `GetUAStatus(nodeID string) Response[map[string]interface{}]`:
           - Call uaManager.GetStatus(nodeID)
           - Return status as map with nodeID, transport, active fields
         - `ListUAStatuses() Response[[]map[string]interface{}]`:
           - Call uaManager.ListActive()
           - Return list of status maps
       - Add SIP trace toggle:
         - `SetSIPTrace(enabled bool) Response[bool]`:
           - Set `sip.SIPDebug = enabled` (import "github.com/emiago/sipgo/sip")
           - Return success

    2. Update `app.go`:
       - Import `sipflow/internal/infra/sip`
       - In `NewApp()`:
         - Create SIPTraceHandler with callback that calls emitter.Emit("sip:trace", entry)
         - Create slog.Logger with the SIPTraceHandler
         - Create UAManager with this logger
         - Pass uaManager to NewSIPService
       - In `shutdown()`:
         - Call `a.sipService.StopAllUAs()` BEFORE `a.projectService.CloseProject()`

    3. Verify: `go build ./...`
  </action>
  <verify>
    - `go build ./...` succeeds
    - SIPService has StartUA, StopUA, StopAllUAs, GetUAStatus, ListUAStatuses, SetSIPTrace methods
    - app.go creates UAManager with trace logger and passes to SIPService
    - app.go shutdown calls StopAllUAs before CloseProject
  </verify>
  <done>UAManager is wired into SIPService. Starting a UA creates a diago instance, stopping destroys it. App shutdown cleans up all UAs. SIP trace toggle controls debug output.</done>
</task>

<task type="auto">
  <name>Task 2: Replace serverStore mock data with backend calls, add SIP trace panel</name>
  <files>
    frontend/src/stores/serverStore.ts
    frontend/src/components/SIPTracePanel.tsx
    frontend/src/components/FlowCanvas.tsx
  </files>
  <action>
    1. Rewrite `frontend/src/stores/serverStore.ts`:
       - Remove all mock data
       - Change SIPServer interface: `id` from string to number (matches ent auto-increment int)
       - Add async actions that call Wails Go bindings:
         - `fetchServers: async () => void` -- calls `window.go.handler.SIPService.ListServers()`, sets servers from response.data
         - `createServer: async (req) => Promise<number | null>` -- calls CreateServer, on success re-fetches list, returns new ID
         - `updateServer: async (id, req) => Promise<boolean>` -- calls UpdateServer, on success re-fetches list
         - `deleteServer: async (id) => Promise<boolean>` -- calls DeleteServer, on success re-fetches list
       - Keep `addServer`/`removeServer`/`updateServer` sync actions removed (replaced by async)
       - On project:opened event, auto-fetch servers (add event listener in a useEffect or call fetchServers from projectStore)

    2. Update any component that uses serverStore with `id: string` to use `id: number`:
       - Check SIPInstancePanel (from Phase 2) -- update server selection to use numeric IDs
       - The server dropdown should call `fetchServers` on mount if servers list is empty

    3. Create `frontend/src/components/SIPTracePanel.tsx`:
       - A collapsible bottom panel (like a console/terminal) showing SIP trace messages
       - Listen for `sip:trace` Wails event using EventsOn
       - State: array of trace entries { time: string, level: string, message: string, nodeID?: string }
       - Display as scrollable monospace text area with auto-scroll to bottom
       - Max 500 entries (drop oldest when exceeding)
       - Clear button to reset trace log
       - Toggle button to enable/disable SIP trace (calls SIPService.SetSIPTrace)
       - Use Tailwind: fixed height (200px), bg-gray-900, text-green-400, font-mono, text-xs
       - Clean up EventsOn in useEffect cleanup

    4. Add SIPTracePanel to the main layout:
       - In FlowCanvas.tsx or App.tsx (whichever manages the main layout), add SIPTracePanel below the canvas area
       - Make it collapsible (hidden by default, toggle button in header or bottom bar)

    5. Verify: `cd frontend && npm run build` passes
  </action>
  <verify>
    - `cd /Users/kyh0703/Project/sipflow/frontend && npm run build` succeeds
    - serverStore.ts has no mock data, uses async Wails binding calls
    - SIPTracePanel.tsx exists and listens for sip:trace events
    - SIPTracePanel is rendered in the app layout
  </verify>
  <done>Frontend serverStore fetches real data from backend. SIP trace panel displays trace messages. No mock data remains.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    Complete SIP infrastructure: server config CRUD (persisted in SQLite), UA lifecycle management (create/destroy diago instances), and SIP trace panel.
  </what-built>
  <how-to-verify>
    1. Launch app with `wails dev`
    2. Create or open a .sipflow project
    3. Check that the SIP server list is initially empty (mock data removed)
    4. If a "manage servers" UI exists, add a test server (name: "Test", address: "127.0.0.1", port: 5060, transport: UDP)
       - If no dedicated management UI yet, verify via browser devtools: `window.go.handler.SIPService.CreateServer({name:"Test", address:"127.0.0.1", port:5060, transport:"UDP", username:"", password:""})` returns success
    5. Verify server persists: close and reopen the project, server should still be in list
    6. Check SIP trace panel is visible (may need to toggle open)
    7. Enable SIP trace toggle in the panel
    8. Check that no errors appear in the console
    9. Close the app cleanly (no hanging processes)
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues</resume-signal>
</task>

</tasks>

<verification>
- `go build ./...` passes
- `go test ./internal/infra/sip/... -v -race` all pass (from Plan 02)
- `cd frontend && npm run build` passes
- App launches, server CRUD works, trace panel renders
- App shuts down cleanly (no leaked goroutines/ports)
</verification>

<success_criteria>
- SIP server configs persist in SQLite across app restarts
- StartUA creates diago instance, StopUA destroys it
- StopAllUAs on shutdown cleans everything
- SIP trace panel displays in UI and receives trace events
- No mock data remains in serverStore
</success_criteria>

<output>
After completion, create `.planning/phases/04-sip-infrastructure/04-03-SUMMARY.md`
</output>
