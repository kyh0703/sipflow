-- return {
--   -- 설치
--   {
--     'Mofiqul/dracula.nvim',
--     lazy = false, -- 컬러스킴은 즉시 로드
--     priority = 1000, -- 먼저 로딩
--     opts = {
--       transparent_bg = true, -- 투명 배경
--     },
--     config = function(_, opts)
--       require('dracula').setup(opts)
--       vim.cmd.colorscheme 'dracula'
--     end,
--   },
-- }
